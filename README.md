# ucsschool ci utils

This repository contains gitlab CI components that are used by some of the repositories of the UCS@school team.

## app-release

The `app-release` component is documented [here](templates/app-release/README.md).
